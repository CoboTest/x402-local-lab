# x402 Audit-Style Run Report

- Run Time: {{meta.timestamp}}
- Resource URL: {{meta.resourceUrl}}
- Facilitator URL: {{meta.facilitatorUrl}}
- Duration (ms): request1={{meta.durationMs.request1}}, request2={{meta.durationMs.request2}}, total={{meta.durationMs.total}}

## 1) Request

### 1.1 First Request
- Method: {{http.firstRequest.method}}
- URL: {{http.firstRequest.url}}
- Response Status: {{http.firstResponse.status}}
- PAYMENT-REQUIRED (raw): `{{http.firstResponse.headers.paymentRequired}}`
- PAYMENT-REQUIRED (decoded):

```json
{{http.firstResponse.paymentRequiredDecoded.json}}
```

### 1.2 Second Request
- Method: {{http.secondRequest.method}}
- URL: {{http.secondRequest.url}}
- Response Status: {{http.secondResponse.status}}
- PAYMENT-SIGNATURE (raw): `{{http.secondRequest.headers.PAYMENT-SIGNATURE}}`

## 2) Signature

- Payer Address: {{addresses.payer}}
- Signature Object:

```json
{{signing.signatureObject.json}}
```

- Payment Payload:

```json
{{signing.paymentPayload.json}}
```

- Signature Result (payload.signature): `{{signing.signatureHex}}`

## 3) Settlement

- PAYMENT-RESPONSE (raw): `{{http.secondResponse.headers.paymentResponse}}`
- PAYMENT-RESPONSE (decoded):

```json
{{http.secondResponse.paymentResponseDecoded.json}}
```

- Settlement txHash: `{{settlement.txHash}}`
- Network: {{http.secondResponse.paymentResponseDecoded.network}}
- Payer (facilitator response): {{http.secondResponse.paymentResponseDecoded.payer}}

## 4) On-chain

- txHash: `{{settlement.txHash}}`
- receipt.status: {{settlement.txReceipt.status}}
- receipt.blockNumber: {{settlement.txReceipt.blockNumber}}
- receipt.from: {{settlement.txReceipt.from}}
- receipt.to: {{settlement.txReceipt.to}}
- receipt.gasUsed: {{settlement.txReceipt.gasUsed}}
- receipt.effectiveGasPrice: {{settlement.txReceipt.effectiveGasPrice}}
- receipt.logs count: {{settlement.logsCount}}

### Balances
- Before: ETH {{balances.before.eth}}, USDC {{balances.before.usdc}} (raw {{balances.before.usdcRaw}})
- After: ETH {{balances.after.eth}}, USDC {{balances.after.usdc}} (raw {{balances.after.usdcRaw}})

---

> Generated by scripts/render-audit-report.mjs from JSON run artifact.
