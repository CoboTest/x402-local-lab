{
  "name": "x402-local-lab",
  "private": true,
  "version": "0.1.0",
  "description": "Local x402 server/client lab",
  "packageManager": "pnpm@10.0.0",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}
